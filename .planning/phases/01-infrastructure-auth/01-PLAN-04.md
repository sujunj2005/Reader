---
wave: 4
depends_on: [01-PLAN-01.md, 01-PLAN-02.md, 01-PLAN-03.md]
files_modified:
  - docker-compose.yml
  - backend/Dockerfile
  - frontend/Dockerfile
  - frontend/nginx.conf
  - .env.example
autonomous: true
requirements_addressed: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
---

# Plan 04: Docker 部署

## Objective

创建 Docker Compose 配置，实现一键部署所有服务（后端、前端、数据库），包含生产级 Dockerfile 和 Nginx 配置。

## Tasks

### Task 1: 创建后端 Dockerfile

<read_first>
- `backend/requirements/base.txt`
- `backend/requirements/dev.txt`
- `backend/src/main.py`
</read_first>

<action>
创建 backend/Dockerfile：

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    gcc \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements first for better caching
COPY requirements/ ./requirements/
RUN pip install --no-cache-dir -r requirements/base.txt

# Copy application code
COPY src/ ./src/
COPY alembic/ ./alembic/
COPY alembic.ini .

# Create non-root user
RUN useradd -m appuser && chown -R appuser:appuser /app
USER appuser

EXPOSE 8000

CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
```
</action>

<acceptance_criteria>
- backend/Dockerfile 使用 python:3.11-slim 基础镜像
- 安装 gcc 和 libpq-dev 系统依赖
- 先复制 requirements/ 再 pip install（利用 Docker 缓存）
- 使用非 root 用户 appuser 运行
- 暴露 8000 端口
- CMD 使用 uvicorn 启动 src.main:app
</acceptance_criteria>

---

### Task 2: 创建前端 Dockerfile 和 Nginx 配置

<read_first>
- `frontend/package.json`
- `frontend/vite.config.js`
</read_first>

<action>
创建 frontend/Dockerfile（多阶段构建）：

```dockerfile
# Build stage
FROM node:20-alpine AS build

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Production stage
FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

创建 frontend/nginx.conf：

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;

    # SPA fallback - all routes go to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API proxy
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static assets caching
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```
</action>

<acceptance_criteria>
- frontend/Dockerfile 使用多阶段构建（build + production）
- 构建阶段使用 node:20-alpine
- 生产阶段使用 nginx:alpine
- COPY --from=build 复制 dist 到 nginx
- frontend/nginx.conf 包含 SPA 路由回退 (try_files)
- nginx 配置 API 代理到 backend:8000
- nginx 启用 gzip 压缩
- 静态资源设置长期缓存 (expires 1y)
</acceptance_criteria>

---

### Task 3: 创建 Docker Compose 配置

<read_first>
- `backend/.env.example`
- `backend/Dockerfile`
- `frontend/Dockerfile`
</read_first>

<action>
创建 docker-compose.yml：

```yaml
version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: novel-reader-db
    environment:
      POSTGRES_DB: novel_reader
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: novel-reader-backend
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:${DB_PASSWORD:-postgres}@db:5432/novel_reader
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-your-secret-key-change-in-production}
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 15
      REFRESH_TOKEN_EXPIRE_DAYS: 7
      CORS_ORIGINS: '["http://localhost:3000", "http://localhost:80"]'
      API_DEBUG: ${API_DEBUG:-False}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend/src:/app/src
    command: >
      sh -c "
        alembic upgrade head &&
        uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
      "

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: novel-reader-frontend
    ports:
      - "80:80"
      - "3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:
```
</action>

<acceptance_criteria>
- docker-compose.yml 包含 db, backend, frontend 三个服务
- db 使用 postgres:15-alpine，包含 healthcheck
- backend 使用自定义 Dockerfile，环境变量配置数据库连接
- backend 启动时先执行 alembic upgrade head
- frontend 映射端口 80 和 3000
- 定义 postgres_data 持久化卷
- 所有服务使用 container_name 命名
</acceptance_criteria>

---

### Task 4: 创建根目录环境变量模板

<read_first>
- `backend/.env.example`
- `docker-compose.yml`
</read_first>

<action>
创建 .env.example（根目录）：

```env
# Database
DB_PASSWORD=postgres

# JWT
JWT_SECRET_KEY=your-super-secret-key-change-this

# API
API_DEBUG=True
```

同时更新 frontend/.env.example：
```env
VITE_API_URL=http://localhost:8000
```
</action>

<acceptance_criteria>
- .env.example（根目录）包含 DB_PASSWORD, JWT_SECRET_KEY, API_DEBUG
- frontend/.env.example 包含 VITE_API_URL
- JWT_SECRET_KEY 默认值为 "your-super-secret-key-change-this"
</acceptance_criteria>

## Verification

### Must Haves
- **Truth**: Docker Compose 包含 db, backend, frontend 三个服务
- **Truth**: 数据库使用 postgres:15-alpine 镜像
- **Truth**: 后端启动时执行 alembic 迁移
- **Truth**: 前端 Nginx 配置 API 代理到后端

### Verification Criteria
1. `grep "services:" docker-compose.yml` - 返回 services 定义
2. `grep "postgres:15-alpine" docker-compose.yml` - 返回 PostgreSQL 镜像
3. `grep "alembic upgrade" docker-compose.yml` - 返回迁移命令
4. `grep "proxy_pass" frontend/nginx.conf` - 返回 API 代理配置
5. `grep "healthcheck" docker-compose.yml` - 返回数据库健康检查
6. `test -f backend/Dockerfile` - 返回 0（文件存在）
7. `test -f frontend/Dockerfile` - 返回 0（文件存在）
