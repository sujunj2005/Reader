---
wave: 1
depends_on: []
files_modified:
  - backend/src/config.py
  - backend/src/database.py
  - backend/src/main.py
  - backend/src/models.py
  - backend/alembic/
  - backend/requirements/base.txt
autonomous: true
requirements_addressed: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
---

# Plan 01: 后端基础设施

## Objective

搭建 FastAPI 后端项目基础架构，配置 PostgreSQL 数据库连接、SQLAlchemy ORM 和 Alembic 迁移系统。

## Tasks

### Task 1: 创建项目结构和依赖

<read_first>
- `.planning/phases/01-infrastructure-auth/01-CONTEXT.md` (decisions)
- `.planning/phases/01-infrastructure-auth/01-RESEARCH.md` (project structure)
</read_first>

<action>
创建以下目录结构和文件：

```
backend/
├── alembic/
│   ├── versions/
│   ├── env.py
│   └── script.py.mako
├── src/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   └── models.py
├── requirements/
│   ├── base.txt
│   └── dev.txt
├── .env.example
└── Dockerfile
```

backend/requirements/base.txt 内容：
```
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.12.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
```

backend/requirements/dev.txt 内容：
```
-r base.txt
pytest>=7.4.0
httpx>=0.25.0
pytest-asyncio>=0.21.0
```
</action>

<acceptance_criteria>
- backend/requirements/base.txt 包含 fastapi, uvicorn, sqlalchemy, asyncpg, alembic, python-jose, passlib, pydantic
- backend/src/ 目录存在 __init__.py, main.py, config.py, database.py, models.py
- backend/alembic/ 目录存在 env.py
- backend/.env.example 存在
</acceptance_criteria>

---

### Task 2: 创建配置模块

<read_first>
- `backend/requirements/base.txt`
- `.planning/phases/01-infrastructure-auth/01-RESEARCH.md` (Pydantic BaseSettings)
</read_first>

<action>
创建 backend/src/config.py，使用 Pydantic BaseSettings：

```python
from pydantic_settings import BaseSettings
from functools import lru_cache

class Settings(BaseSettings):
    # API
    API_DEBUG: bool = False
    API_NAME: str = "Novel Reader API"
    API_VERSION: str = "0.1.0"
    API_HOST: str = "0.0.0.0"
    API_PORT: int = 8000
    
    # Database
    DATABASE_URL: str = "postgresql+asyncpg://postgres:postgres@db:5432/novel_reader"
    
    # JWT
    JWT_SECRET_KEY: str = "your-secret-key-change-in-production"
    JWT_ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 15
    REFRESH_TOKEN_EXPIRE_DAYS: int = 7
    
    # CORS
    CORS_ORIGINS: list[str] = ["http://localhost:3000"]
    
    class Config:
        env_file = ".env"

@lru_cache()
def get_settings() -> Settings:
    return Settings()
```
</action>

<acceptance_criteria>
- backend/src/config.py 包含 class Settings(BaseSettings)
- Settings 包含 API_DEBUG, DATABASE_URL, JWT_SECRET_KEY, JWT_ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES, REFRESH_TOKEN_EXPIRE_DAYS, CORS_ORIGINS
- get_settings() 函数存在并使用 @lru_cache()
- 默认值与 ACTION 中的一致
</acceptance_criteria>

---

### Task 3: 创建数据库模块

<read_first>
- `backend/src/config.py`
- `.planning/phases/01-infrastructure-auth/01-RESEARCH.md` (SQLAlchemy async engine)
</read_first>

<action>
创建 backend/src/database.py：

```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
from sqlalchemy.orm import DeclarativeBase
from .config import get_settings

settings = get_settings()

engine = create_async_engine(
    settings.DATABASE_URL,
    echo=settings.API_DEBUG,
    pool_pre_ping=True,
)

async_session_maker = async_sessionmaker(
    engine,
    class_=AsyncSession,
    expire_on_commit=False,
)

Base = DeclarativeBase()

# Naming convention for constraints
Base.metadata.naming_convention = {
    "ix": "ix_%(column_0_label)s",
    "uq": "uq_%(table_name)s_%(column_0_name)s",
    "ck": "ck_%(table_name)s_%(constraint_name)s",
    "fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s",
    "pk": "pk_%(table_name)s"
}

async def get_db() -> AsyncSession:
    async with async_session_maker() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

创建 backend/src/models.py：
```python
from .database import Base

# Global models will be imported here
__all__ = ["Base"]
```
</action>

<acceptance_criteria>
- backend/src/database.py 包含 create_async_engine, async_sessionmaker, Base = DeclarativeBase()
- engine 使用 settings.DATABASE_URL
- async_session_maker 使用 class_=AsyncSession, expire_on_commit=False
- get_db() 异步生成器存在，包含 try/except 回滚逻辑
- Base.metadata.naming_convention 包含 ix, uq, ck, fk, pk
- backend/src/models.py 从 database 导入 Base
</acceptance_criteria>

---

### Task 4: 创建 FastAPI 主应用

<read_first>
- `backend/src/config.py`
- `backend/src/database.py`
</read_first>

<action>
创建 backend/src/main.py：

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from .config import get_settings

settings = get_settings()

app = FastAPI(
    title=settings.API_NAME,
    version=settings.API_VERSION,
    debug=settings.API_DEBUG,
)

# CORS Middleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/health")
async def health_check():
    return {"status": "ok", "version": settings.API_VERSION}
```

创建 backend/alembic.ini（模板）：
```ini
[alembic]
script_location = alembic
sqlalchemy.url = postgresql+asyncpg://postgres:postgres@db:5432/novel_reader

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
```

创建 backend/alembic/env.py（基础模板）：
```python
from logging.config import fileConfig
from sqlalchemy import engine_from_config, pool
from alembic import context
from src.database import Base
from src.config import get_settings

config = context.config
config.set_main_option("sqlalchemy.url", get_settings().DATABASE_URL.replace("+asyncpg", ""))

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata

def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(url=url, target_metadata=target_metadata, literal_binds=True)
    with context.begin_transaction():
        context.run_migrations()

def run_migrations_online() -> None:
    connectable = engine_from_config(
        config.get_section(config.config_ini_section),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    with connectable.connect() as connection:
        context.configure(connection=connection, target_metadata=target_metadata)
        with context.begin_transaction():
            context.run_migrations()

if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```
</action>

<acceptance_criteria>
- backend/src/main.py 包含 app = FastAPI() 和 health_check 端点
- main.py 包含 CORSMiddleware 配置，allow_credentials=True
- backend/alembic.ini 存在且包含 [alembic] section
- backend/alembic/env.py 导入 Base 和 get_settings
- env.py 设置 target_metadata = Base.metadata
- env.py 包含 run_migrations_offline 和 run_migrations_online 函数
</acceptance_criteria>

---

### Task 5: 创建环境变量模板

<read_first>
- `backend/src/config.py`
</read_first>

<action>
创建 backend/.env.example：
```env
# API Settings
API_DEBUG=True
API_NAME=Novel Reader API
API_VERSION=0.1.0
API_HOST=0.0.0.0
API_PORT=8000

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/novel_reader

# JWT
JWT_SECRET_KEY=your-super-secret-key-change-this
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
CORS_ORIGINS=["http://localhost:3000"]
```
</action>

<acceptance_criteria>
- backend/.env.example 包含 API_DEBUG, DATABASE_URL, JWT_SECRET_KEY, JWT_ALGORITHM
- JWT_SECRET_KEY 值为 "your-super-secret-key-change-this"
- ACCESS_TOKEN_EXPIRE_MINUTES=15
- REFRESH_TOKEN_EXPIRE_DAYS=7
- CORS_ORIGINS 包含 "http://localhost:3000"
</acceptance_criteria>

## Verification

### Must Haves
- **Truth**: 所有核心文件都已创建并可导入
- **Truth**: 数据库连接使用异步引擎 (asyncpg)
- **Truth**: JWT 配置包含 ACCESS_TOKEN_EXPIRE_MINUTES=15 和 REFRESH_TOKEN_EXPIRE_DAYS=7

### Verification Criteria
1. `grep "fastapi" backend/requirements/base.txt` - 返回 fastapi
2. `grep "class Settings" backend/src/config.py` - 返回 Settings 类
3. `grep "create_async_engine" backend/src/database.py` - 返回异步引擎
4. `grep "CORSMiddleware" backend/src/main.py` - 返回 CORS 配置
5. `test -f backend/alembic/env.py` - 返回 0 (文件存在)
